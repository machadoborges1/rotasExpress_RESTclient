/** 
const express = require('express');

const app = express();

app.get("/primeira-rota", (request, response) => {
  return response.send("primeira rota");  //retornou mensagem ---- primeira rota
});



app.listen(4003, ()=> console.log('porta 4002'));
*/
